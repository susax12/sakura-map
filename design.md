# 桜マップ デザインガイド

`drone-entertainment.co.jp` のブランドを踏襲し、桜ドローンプロジェクトの世界観で「全国 桜マップ」を構築するためのデザイン仕様。会社HPへ実装する際の基準ドキュメント。

> 出典: 会社HP (`drone-entertainment.co.jp`) の実CSS / ロゴ / コピーから抽出（2026-05-29 時点）。

---

## 1. ブランドの位置づけ

- 会社HPに既に **「Sakura Drone Project」** のメニュー項目が存在 → 本ページはその配下コンテンツとして世界観を揃える
- メインキャッチ: **「遊び」が「仕事」に。** ／ 最新テクノロジーを活用した映像表現で社会を元気にする
- トーン: モダン × 親しみやすい「次世代ポップ・テック」。堅すぎず、技術志向と先進性を出す
- 桜ページ単体では、ブランドの骨格（白／ダーク／赤）を保ちつつ、テーマ色として桜ピンクを装飾に効かせる

---

## 2. カラーパレット

| 役割 | HEX | 用途 |
|------|-----|------|
| Brand Red（主アクセント） | `#DA0000` | CTA・アクティブ状態・リンク・県名バッジ。HPで最も多用される差し色 |
| Ink（基調テキスト） | `#222222` | 本文・見出し |
| Dark（ダーク面） | `#1A1A1A` 〜 `#222` | ヘッダー・フッター・ボタン背景 |
| White | `#FFFFFF` | 基調背景 |
| BG Alt（淡グレー） | `#F6F6F6` | セクション区切りの薄背景 |
| Muted（補助グレー） | `#8C8F9A` / `#434960` | 補助テキスト・メタ情報 |
| Line | `#E7E7EA` | 境界線・チップの枠 |

### テーマ色（桜ページ専用・装飾用）

| 役割 | HEX | 用途 |
|------|-----|------|
| Sakura | `#F7A8C4` | 淡い桜ピンク（補助装飾） |
| Sakura Deep | `#E26D9B` | ピン・クラスタ・桜系タグ |

**設計判断**: 赤（`#DA0000`）は桜の主役には強すぎるため、地図ピンは桜ピンク（`#E26D9B`）を採用。赤はブランド差し色として CTA・アクティブ・リンク・県名バッジに限定使用 → 会社ブランドと桜テーマを両立。

---

## 3. タイポグラフィ

会社HPと完全一致のスタックを使用（Google Fonts）。

```
英字: "Lato", sans-serif          (wght 400/700/900)
和文: "Noto Sans JP", "Hiragino Kaku Gothic ProN", sans-serif  (wght 400/500/700)
```

- ロゴ・英字ラベル（`SAKURA DRONE PROJECT` / `SPOTS` 等）: **Lato 900**、`letter-spacing: .05–.08em` で引き締める
- 和文見出し・本文: Noto Sans JP 700 / 400
- 英字には軽いトラッキング（字間）を入れるのがHPの特徴

---

## 4. レイアウト原則

- **ダークヘッダー固定**（高さ 64px、`#1A1A1A`）: 左にロゴ（英字＋和文サブ）、右に spot 数カウンタ
- 余白を十分に取り、要素間にはっきりした区切り
- カード型・チップ型UI（HPの WORKS/NEWS と同じ語彙）
- 角丸は控えめ（チップは pill、パネル・ボタンは 8–12px）
- 影は浅め・自然（`0 2px 8px rgba(0,0,0,.1)` 程度）

---

## 5. コンポーネント仕様（桜マップ）

| 要素 | 仕様 |
|------|------|
| ヘッダー | ダーク `#1A1A1A` / 左に会社公式ロゴ `logo_de.svg`（白テキスト＋赤ドローンアイコン、公式 `logo.svg` の配色に準拠。`assets/` にローカル配置）/ 区切り線 + 桜マップ見出し |
| 地図 | Leaflet + **地理院タイル（淡色）**。日本語ラベル・官公庁系で上品。起動時はピンに fitBounds、パン範囲は日本周辺に制限 |
| ピン | **YouTubeサムネ**を桜ピンク `#E26D9B` 枠の角丸サムネ + 🌸バッジで表示（一覧性UP）。密集地はクラスタ表示 |
| 共有 | `?spot=<videoId>` ディープリンクで特定スポットを直接開ける（SNS/メール共有用） |
| 地方フィルタ | pill チップ。非選択=白＋グレー枠、選択=`#DA0000` 塗り |
| 詳細パネル | 右からスライドイン（モバイルは下からボトムシート）。上に16:9動画、下に県名バッジ＋桜名＋市町村＋赤CTAボタン。同一スポットに複数動画がある場合は動画切替サムネ・ストリップを表示 |
| 動画集約 | 同一スポットの複数動画（複数年・Short/Long等）は1ピンに集約。ピンに本数バッジ、パネルでサムネ切替。誤併合防止のため「名称コア一致＋座標近接」で確信できる6グループ(15動画)のみ手動マージ |
| CTAボタン | `#DA0000` 塗り / 白文字 / hover `#B80000` |
| 一覧 | 地方ごとにグループ化したリスト。クリックで地図がその地点へ fly |

---

## 6. 会社HP実装時の移植方針

現状は単一HTML（CDN依存）プロトタイプ。品質確定後、HP（WordPress 構成）への移植は以下のいずれか：

1. **iframe 埋め込み**（最速・低リスク）: プロトタイプを `sakura-map/` として配置し、固定ページに iframe。改修が本体と独立 ◎
2. **固定ページ直接実装**: HPの既存 CSS/フォントは流用済みなので、Leaflet 一式をテーマに読み込み、本HTMLの `<body>` 以下を固定ページテンプレートへ移植
3. **ショートコード化**: WordPress プラグイン or functions.php でショートコード `[sakura_map]` を定義

推奨は **(1) iframe** での先行公開 → 反応を見て (2) に寄せる（推奨度 70%）。理由: 本体テーマと衝突せず、桜シーズン限定での出し入れが容易。

---

## 7. データ連携メモ

- ソース: YouTube `@drone-entertainment` の**全公開動画152本**から抽出 → 地図掲載 **134スポット**
- 除外16本: オンライン花見(2) / プロジェクト紹介・プロモ・総集編(8) / 非桜のFPV・マイクロドローン撮影(6)。いずれも単一地点に紐付かない or 桜映像でないため地図ピン化の対象外
- 各動画タイトル形式: `[SAKURA DRONE 4K] 桜名（English）都道府県市町村｜桜ドローンプロジェクト`
- 座標は市町村レベルの概算（Nominatim ジオコーディング）。**正確な木の位置への補正は要対応**（精度 70%）
- 同一スポットの複数動画は1ピンに集約済み（`index.html` の `MERGE` 配列で定義）。地図ピン134動画 → 表示125スポット
  - 集約6グループ: 富山・春の四重奏(3) / 静岡・潤井川龍巌淵(2) / 和歌山・古座川クマノザクラ(3) / 広島・佐木島塔ノ峰千本桜(3) / 香川・紫雲出山(2) / 高知・仁淀ブルー(2)
  - 表記ゆれが大きく自動検出しにくい同一スポットは要目視。追加で集約したい場合は `MERGE` に `{ '動画ID':'ラベル', ... }` を1行追加
- 動画追加時は `index.html` 内 `const SPOTS = [...]` に1行追加するだけ
- 将来: YouTube Data API でチャンネル新着を自動取り込み → SPOTS 自動更新も可（要APIキー）

---

## 8. 未確定・要判断（※要検証）

- [ ] 座標精度: 市町村中心 → 実際の桜の木の位置に補正するか（同一市町村に複数本ある地点が現状ジッターで分散）
- [ ] 公開範囲: HP固定ページ / 限定公開 / 桜シーズンのみ掲出
- [ ] 英語併記（インバウンド向け。タイトルに英名あり、対応容易）
- [ ] 開花度・撮影日のメタ表示要否
- [ ] AI検索対応（公開系なら `ai-discoverability` skill で audit 推奨）
